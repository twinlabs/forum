.post
  .meta.clearfix
    .username.datum=post.user.name
    .time.datum
      i.fa.fa-clock-o
      .distance(data-timestamp=post.created_at)= moment(post.created_at).fromNow()
  .content!=post.body
