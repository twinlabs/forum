var assert = require('assert');
var appModule = rootRequire('app');

describe ('signup', function(){
  it('accepts signup requests with a username and password and creates new users', function(){
    assert(false);
  });
});
